{
  "name": "personal-blog",
  "version": "1.0.0",
  "description": "个人博客项目 - 使用Firebase后端",
  "main": "index.html",
  "scripts": {
    "dev": "live-server --port=3000",
    "build": "node build-optimize.js",
    "build:analyze": "node build-optimize.js && echo 'Build analysis complete'",
    "optimize": "node build-optimize.js",
    "deploy": "npm run build && echo 'Ready for Netlify deployment'",
    "perf:test": "echo 'Performance testing - check browser dev tools'",
    "perf:report": "echo 'Performance report generated - check console'"
  },
  "keywords": [
    "blog",
    "personal",
    "firebase",
    "netlify"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "firebase": "^12.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "live-server": "^1.2.2",
    "playwright": "^1.55.0"
  }
}
